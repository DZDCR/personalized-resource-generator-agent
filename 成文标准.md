# 成文标准

> 本文件定义本课程项目（个性化资源生成与学习多智能体系统）所有技术文档应满足的成文规范，适用于架构设计、API 文档、数据字典、开发规范、Git 协作规范五类文档。

---

## 一、通用成文要求

### 1.1 文档格式

| 项 | 要求 |
|----|------|
| 格式 | Markdown（`.md`），文件名全英文小写+连字符 |
| 语言 | 中文为主，技术术语保留英文原词（如 `Spring Boot`、`DTO`） |
| 版本 | 文件头部注明版本号、日期、状态（草案/正式） |
| 目录 | 超过 500 字的文档须提供目录或分节导航 |
| 图表 | 架构图/流程图使用 Markdown Mermaid 语法或图片，禁止纯文字画图 |

### 1.2 内容质量

| 等级 | 标准 |
|------|------|
| 优秀 | 有图表 + 伪代码示例 + 正反对照 + 技术选型理由 + 边界说明 |
| 合格 | 有结构化文字，条理清晰，无明显遗漏 |
| 不合格 | 无结构，堆砌式文字，缺图表，缺示例 |

### 1.3 更新维护

- 文档与代码同步更新，代码变更时文档须同时修订
- 重大变更须在文档末尾变更记录表中登记
- 废弃内容标注删除线或 `~~废弃~~`，不可直接删除

---

## 二、架构设计文档成文标准

### 2.1 必含章节

| 章节 | 内容 | 权重 |
|------|------|------|
| 系统目标与非功能需求 | 性能/可用性/可扩展性指标 | 10% |
| 总体架构图 | 分层架构 + 模块关系，用图表达 | 20% |
| 分层与依赖规则 | 每层职责 + 禁止项 | 15% |
| 核心业务流程 | 时序图/流程图（多智能体编排） | 20% |
| 数据架构 | ER 关系图 + 分区/索引策略 | 10% |
| 技术选型 | 对比表 + 选型理由 + 取舍说明 | 15% |
| 部署架构 | 部署拓扑图 + 环境变量清单 | 10% |

### 2.2 图表要求

- 架构图使用分层框图，每层注明技术栈
- 流程图使用 Mermaid 语法，标注异常分支
- 数据流用箭头标注方向，关键数据格式标注于线上
- 每张图须有图注说明

### 2.3 必须体现

- [ ] 多智能体协作架构（角色/职责/通信方式）
- [ ] 画像维度定义（≥6 维度）
- [ ] 个性化路径规划算法伪代码
- [ ] LLM 调用失败的容错策略
- [ ] 非功能性指标（延迟/并发/可用性）

---

## 三、API 文档成文标准

### 3.1 单个接口必须包含

```
### POST /api/{resource}
功能说明：一段话描述该接口作用

**请求**
- 方法 + 路径
- Content-Type
- 请求体字段表（字段名 / 类型 / 必填 / 说明）
- 请求体 JSON 示例

**响应**
- 成功响应字段表 + 示例
- 错误响应（code / message 含义）
- 错误码汇总表

**备注**
- 限流/鉴权要求（如有）
- 关联接口说明
```

### 3.2 完整性要求

| 接口类型 | 必含示例 |
|----------|---------|
| 查询接口 | GET 带参数 + 空结果返回 |
| 写入接口 | POST 请求体 + 响应体 |
| 异步接口 | 提交任务号 + 进度查询方式 |
| 错误场景 | 参数缺失/资源不存在/服务异常 |

### 3.3 RESTful 规范

- 资源名用复数名词（`/resources`，非 `/resource`）
- 操作用 HTTP 方法区分（GET/POST/PUT/DELETE）
- 嵌套资源用路径层级（`/paths/{id}/nodes`）
- 状态码语义正确：200 成功 / 400 参数错误 / 404 不存在 / 500 服务异常

---

## 四、数据字典成文标准

### 4.1 每张表必须包含

| 列项 | 说明 |
|------|------|
| 表名 | 下划线复数（`quiz_records`） |
| 说明 | 一段话描述表用途 |
| 列清单 | 列名 / 类型 / 约束（PK/NN/UQ/默认值）/ 说明 |
| 索引 | 列出主要索引及用途 |

### 4.2 一致性要求

- 表名与 Entity 类对应关系明确
- 列名（snake_case）与 Java 字段（camelCase）对应关系明确
- 枚举值与代码中的枚举类一致
- JSON 字段结构有独立说明或 Schema

### 4.3 必须体现

- [ ] 主键策略（自增/雪花/UUID）
- [ ] 外键策略（物理外键 / 逻辑关联，说明原因）
- [ ] 时间字段统一 UTC 存储
- [ ] 大字段（jsonb/text）使用场景

---

## 五、开发规范成文标准

### 5.1 必含章节

| 章节 | 内容 |
|------|------|
| 工程环境 | JDK/Node/Maven/数据库版本要求 |
| 目录职责 | 每个顶层目录的职责一句话说明 |
| 命名规范 | 包/类/方法/变量/表/列的命名规则 |
| 编码规范 | Java + JavaScript/TypeScript 双语言规范 |
| 分层禁止 | 每层**不能做什么**（比"能做什么"更重要） |
| Prompt 规范 | 模板目录约定 + 占位符格式 + 热更新方式 |
| Review 清单 | 提交前自检项目（Checklist 形式） |

### 5.2 示例要求

- 每条规范至少一个正确示例
- 常见反模式至少一个错误示例
- 示例使用代码块格式，标注语言

---

## 六、Git 协作规范成文标准

### 6.1 必含章节

| 章节 | 内容 |
|------|------|
| 分支模型 | 分支类型 + 命名规则 + 生命周期 |
| 提交信息 | 格式规范 + 类型说明 + 示例 |
| PR/MR 规范 | 标题格式 + 描述模板 + Review 要求 |
| 工作流 | 从创建分支到合并的完整流程 |
| 冲突处理 | rebase vs merge 策略 |

### 6.2 必须体现

- [ ] 主分支保护规则（禁止直接 push）
- [ ] 分支命名示例（feat/fix/docs/refactor）
- [ ] 至少 3 个提交信息示例
- [ ] 分支生命周期（1~3 天合并回主干）

---

## 七、文档命名与存放约定

| 文档类型 | 文件名 | 存放路径 |
|----------|--------|----------|
| 架构设计 | `architecture.md` | `docs/` |
| API 文档 | `api-spec.md` | `docs/` |
| 数据字典 | `data-dictionary.md` | `docs/` |
| 开发规范 | `dev-guide.md` | `docs/` |
| Git 规范 | `git-guide.md` | `docs/` |
| 成文标准 | `成文标准.md` | `docs/` |
| 变更记录 | `changelog.md` | `docs/` |

---

## 八、评分对照表（用于课程作业自评）

| 维度 | 优秀（90+） | 合格（70~89） | 不合格（<70） |
|------|------------|--------------|--------------|
| 结构 | 章节完整、层级清晰、有目录 | 基本结构完整 | 结构混乱 |
| 图表 | 架构图+时序图+流程图+ER图 | 有图但不完整 | 无图或仅文字示意 |
| 示例 | 每条规则有正反对照示例 | 有示例但不够丰富 | 无示例 |
| 选型 | 有对比分析+量化依据 | 有理由但缺对比 | 仅列结论 |
| 一致性 | 文档间交叉引用正确 | 基本一致 | 存在矛盾 |
| 可执行性 | 规则可直接落地，有 Checkstyle 等工具配套 | 有规则但缺工具配套 | 规则模糊无法执行 |

---

## 九、变更记录

| 版本 | 日期 | 作者 | 变更内容 |
|------|------|------|---------|
| v1.0 | 2026-09-14 | 课程项目组 | 初稿，定义五类文档成文标准 |